package com.yuchen.ailedger.ui.gl

/**
 * Marker for the root optical-field fix: droplet refraction must be computed as one
 * continuous capsule field, not as stitched left/middle/right segments.
 */
internal const val OPENGL_DROPLET_CONTINUOUS_FIELD_FIX = true
